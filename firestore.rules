
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ========== HELPER FUNCTIONS ==========

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && request.auth.token.email == "kamandagcudas@gmail.com";
    }
    
    // Validates incoming data for a NEW user profile
    function isCreatingOwnValidProfile(userId) {
      let incomingData = request.resource.data;
      let incomingPlayer = incomingData.player;

      // User must be creating their own document, and cannot start off banned or muted.
      // They also cannot grant themselves creator status unless they are the admin.
      return isOwner(userId) &&
             incomingPlayer.isBanned == false &&
             incomingPlayer.isMuted == false &&
             incomingPlayer.isCreator == (request.auth.token.email == "kamandagcudas@gmail.com");
    }

    // ========== COLLECTION RULES ==========

    // User profiles
    match /users/{userId} {
      // Any signed-in user can read profiles for user directory & leaderboards.
      allow read, list: if isSignedIn();
      
      // A user can create their own profile, but only with valid initial data.
      allow create: if isCreatingOwnValidProfile(userId);
      
      // A user can update their own document, but cannot change protected fields.
      // An admin can update any document.
      allow update: if (
                      (isOwner(userId) &&
                      request.resource.data.player.uid == resource.data.player.uid &&
                      request.resource.data.player.username == resource.data.player.username &&
                      request.resource.data.player.isCreator == resource.data.player.isCreator &&
                      request.resource.data.player.isBanned == resource.data.player.isBanned &&
                      request.resource.data.player.isMuted == resource.data.player.isMuted
                    ) || isAdmin());

      // Only admins can delete user profiles.
      allow delete: if isAdmin();
    }

    // Public feedback board
    match /feedback/{feedbackId} {
      // Any signed-in user can read feedback posts.
      allow read, list: if isSignedIn();

      // Any signed-in user can post feedback.
      allow create: if isSignedIn();

      // Only admins can modify or delete feedback posts.
      allow update, delete: if isAdmin();
    }

    // Login History Logs
    match /loginHistory/{logId} {
      // Only admins can read login history.
      allow read, list: if isAdmin();

      // Any signed-in user can create their own login attempt log.
      allow create: if isSignedIn(); 

      // Logs are immutable.
      allow update, delete: if false;
    }

    // Activity Logs
    match /activityLogs/{logId} {
      // Only admins can read or write activity logs.
      allow read, write, list: if isAdmin();
    }
  }
}
